# Claude Code OAuth Token (for Mode A: plain Docker, Mode C: Fly.io)
#
# Get your token: node extract-token.mjs
# Or auto-extracted from macOS Keychain (Claude Code 2025+)
#
# Mode B (Docker Sandbox) does NOT need this — handled by Docker Desktop proxy.
# Mode C (Fly.io) uses fly secrets, not this file — see npm run fly:push-token.
#
# CLAUDE_CODE_OAUTH_TOKEN=sk-ant-oat01-your-token-here

# Fly.io config (Mode C)
# FLY_APP=your-app-name        ← must match `app` in fly.toml
# FLY_ORG=webhouse             ← or "personal" (fly orgs list to see options)
# FLY_REGION=arn               ← preferred region (default: ams)
