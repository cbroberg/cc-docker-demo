# Claude Code OAuth Token (for Mode A: plain Docker)
#
# Get your token: node extract-token.mjs
# Or auto-extracted from ~/.claude/.credentials.json
#
# Mode B (Docker Sandbox) does NOT need this — credentials are handled
# automatically by Docker Desktop's host-side proxy.
#
# CLAUDE_CODE_OAUTH_TOKEN=sk-ant-oat01-your-token-here
